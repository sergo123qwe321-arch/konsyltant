# Системный журнал (process.md)

**Обязательное правило разработки:** Единый системный журнал проекта ведется СТРОГО в файле `process.md` (создание дублирующих файлов вроде `процесс.txt` запрещено). Журнал `process.md` обновляется после каждого значимого выполненного действия, изменения логики или завершения этапа.
**ВАЖНО:** При ознакомлении с проектом обязательно прочитайте файл ARCHITECTURE.md (технический "Канвас" проекта).

## Текущая структура проекта и архитектура
- **Корень:** `C:\Different\Work\МАЛЕНЬКАЯ_СТРАНА\antigravKONSYLTANT`
- **Целевая среда:** Изолированные Docker-контейнеры (Ubuntu, VPS Beget).
- **Архитектура:** MVP веб-приложения. Бэкенд (FastAPI), Фронтенд (Vanilla HTML/CSS/JS, SPA).
- **Хранение документов:** Яндекс.Диск (облачные папки).
- **База данных (PostgreSQL):** Это строго "таблица маршрутизации/ключей" (хранит токены, хэши паролей и ID папок), медицинские данные в нее не попадают.
- **ИИ (RAG):** GigaChat / OpenRouter.

### ETL Pipeline и Кэширование (Yandex.Disk)
а) Программа сканирует папку и парсит новые файлы (PDF/PNG через OCR, DOCX).
б) Текст разбивается на чанки и сохраняется обратно на Яндекс.Диск в виде файла `_{имя_папки}_cache.json`.
в) Строгое правило: любые файлы, начинающиеся с `_` (подчеркивания), полностью игнорируются сканером, чтобы избежать рекурсии.

### Памятка по Stateless JWT-аутентификации (Frontend & API)
- **Авторизация (POST /api/login):** При успешном входе бэкенд теперь возвращает токен доступа (`access_token` / `session_token`).
- **Передача токена:** В JS при всех последующих запросах к защищенным эндпоинтам (чат `/api/chat` и список файлов `/api/patient/files`) необходимо передавать заголовок:
  ```http
  Authorization: Bearer <access_token>
  ```
- **Обработка 401 Unauthorized (Таймаут 30 минут):** Если пользователь неактивен 30 минут, бэкенд вернет код 401. Фронтенд должен перехватить 401 ошибку, очистить сохраненный токен из `localStorage` / `sessionStorage` и перенаправить пользователя на форму логина.

## Лог выполненных действий
- [2026-07-22] Создано REST API на FastAPI, протестирована система авторизации.
- [2026-07-22] Реализован ИИ-модуль RAG (документы, маршрутизация, промпт).
- [2026-07-22] Реализован UI фронтенда: SPA с премиальным дизайном.
- [2026-07-22] Создан модуль авто-обнаружения `folder_watcher.py` и протестирована рассылка SMTP.
- [2026-07-22] Исправлено чтение нативных документов Google Docs: теперь они корректно экспортируются в `text/plain` и парсятся без ошибки "формат не поддерживается".
- [2026-07-24] Улучшена стабильность соединения с OpenRouter (тайм-ауты, User-Agent, retries, proxies).
- [2026-07-24] Проект подготовлен к деплою на Render.com: обновлены `requirements.txt` и `.gitignore`, добавлен `Procfile`, а в `drive_api.py` встроена авторизация через `GOOGLE_CREDENTIALS_JSON`.
- [2026-07-24] Инициализирован Git-репозиторий. Файлы с секретами и базой данных успешно исключены из индекса. Создан первый коммит со всем кодом.
- [2026-07-27] Успешная авторизация в GitHub CLI и автоматическое создание удаленного репозитория konsyltant. Обнаружены и применены системные настройки прокси (127.0.0.1:9697) для обхода сетевых ограничений. Исходный код успешно загружен в GitHub.
- [2026-07-27] Внедрен динамический BASE_URL для генерации email-ссылок. Изменения зафиксированы и выгружены в GitHub.
- [2026-07-27] Интегрирован запуск фонового отслеживателя (folder_watcher) в жизненный цикл FastAPI (lifespan) с периодичностью раз в минуту. Это обеспечит автоматическую отправку писем на серверах Render. Код выгружен на GitHub.
- [2026-07-27] Добавлено диагностическое логирование в `folder_watcher.py` и реализован рекурсивный поиск (скрипт находит все новые папки, даже созданные внутри подпапок). Код выгружен на GitHub.
- [2026-07-27] Исправлена ошибка инициализации БД на Render (`no such table: patient_access`). Вызов `init_db()` добавлен в lifespan до запуска фоновых процессов. Код выгружен на GitHub.
- [2026-07-27] Усилено логирование SMTP: добавлена полная трассировка подключения и отправки писем, а также мягкая обработка ошибок через try/except. Изменения выгружены на GitHub.
- [2026-07-27] Исправлено критическое зависание отправки писем: SMTP-подключение переведено на порт 465 (SMTP_SSL) с жестким таймаутом 10 секунд. Ошибки сети (`socket.timeout`) теперь перехватываются. Код загружен на GitHub.
- [2026-07-27] Устранена ошибка соединения (`[Errno 101] Network is unreachable`) в облаке Render: теперь при отправке почты функция `getaddrinfo` временно переопределяется для принудительного использования IPv4 (`socket.AF_INET`). Изменения выгружены на GitHub.
- [2026-07-27] Исправлен баг `TypeError` при переопределении `socket.getaddrinfo`: функция-обертка переведена на безопасное использование позиционных аргументов. Код выгружен на GitHub.
- [2026-07-27] Оптимизирована логика подключения SMTP: код переведен на использование контекстного менеджера `with smtplib.SMTP_SSL(...) as server`, а таймаут увеличен до 15 секунд для повышения стабильности на облачных платформах. Код выгружен на GitHub.
- [2026-07-27] 🎉 **MVP УСПЕШНО ЗАПУЩЕН В ПРОДАКШЕН (Render.com)!**
  - Сайт работает, ИИ-Консультант стабильно отвечает.
  - **Как мы этого добились:**
    1) Решили проблемы сети и обхода ограничений облачных провайдеров (таймауты, User-Agent, принудительный IPv4 `socket.AF_INET`).
    2) Стабилизировали фоновую рассылку писем, перейдя на защищенный порт 465 (`SMTP_SSL`) с контекстными менеджерами и 15-секундным таймаутом.
    3) Исправили логику инициализации БД, добавив вызов `init_db()` в жизненный цикл FastAPI, чтобы база автоматически разворачивалась при "холодном" старте на Render.
    4) Внедрили динамический `BASE_URL`, детальное логирование и рекурсивный поиск папок на Диске для надежной фоновой работы сервиса.
- [2026-07-27] Добавлен внутренний "будильник" (keep-awake) для предотвращения засыпания бесплатных инстансов Render. Каждые 10 минут фоновый поток в приложении отправляет HTTP-запрос (ping) на собственный `BASE_URL`. Код выгружен на GitHub.
- [2026-07-27] Устранена ошибка сетевого таймаута (`[WinError 10060]`) при обращении к Google Drive API: в модуль `drive_api.py` внедрена поддержка системных прокси (через `httplib2.ProxyInfo.from_environment()`), а сбои получения файлов теперь перехватываются с понятными для UI-пользователя сообщениями ("Связь с Google Drive временно недоступна"). Код выгружен на GitHub.
- [2026-07-27] Исправлен баг `AttributeError` в модуле `httplib2`: некорректный вызов метода заменен на безопасное извлечение прокси через `proxy_info_from_environment()` с блоком try/except. Код загружен на GitHub.
- [2026-07-27] Проведен аудит эндпоинтов авторизации и переменных окружения:
  - Установлено, что роуты бэкенда (`@app.post("/api/verify-token")` в `main.py`) и фронтенда (`fetch('/api/verify-token')` в `app.js`) **совпадают**.
  - 404 ошибка при авторизации является штатным ответом API на отсутствие токена в базе данных (вероятно, из-за очистки временной файловой системы `konsyltant.db` на Render при перезапуске сервера).
  - Подтверждено, что `BASE_URL` корректно считывается через `os.getenv` для формирования ссылок в email.
- [2026-07-27] Проведена масштабная миграция архитектуры хранения данных: приложение переведено с локальной файловой базы (SQLite) на полноценную облачную PostgreSQL (`psycopg2-binary`). Изменен синтаксис SQL-запросов и внедрен гибридный режим инициализации. Это полностью решает проблему "амнезии" сервера (пропажи токенов и ошибок 404) при перезапусках бесплатного инстанса Render. Код загружен на GitHub.
- [2026-07-27] Внедрены предохранители для облачной среды: жестко зафиксирован порт 465 (`SMTP_SSL`) для фоновой рассылки писем, а также добавлена автоматическая адаптация схемы `postgres://` -> `postgresql://` для безопасного подключения к БД Render. Код выгружен на GitHub.
- [2026-07-27] Выявлено, что бесплатный тариф Render аппаратно блокирует любые исходящие подключения по почтовым портам (25, 465, 587) для предотвращения спама. В качестве экстренного решения внедрен вывод сгенерированных доступов (ссылки и пароля) напрямую в серверные логи `main.py`, что позволяет продолжить тестирование. Код загружен на GitHub.
- [2026-07-27] Выполнено жесткое (безусловное) назначение переменной `SMTP_PORT = 465` в коде `folder_watcher.py`, исключающее возможность ее переопределения через переменные окружения Render. Код выгружен на GitHub.
- [2026-07-27] Осуществлен полный отказ от блокируемого протокола SMTP в пользу современного HTTPS API (443 порт). Отправка писем переведена на облачный сервис **Resend API**. Код модуля `folder_watcher.py` переписан на использование библиотеки `requests` (POST `https://api.resend.com/emails`). Код успешно закоммичен и выгружен на GitHub.
- [2026-07-28] Исправлен баг полноты поиска папок в Google Drive API: в `folder_watcher.py` добавлена встроенная поддержка пагинации (`nextPageToken` и `pageSize=100`), что позволяет обходить дефолтный лимит API (100 элементов) и гарантированно сканировать абсолютно все папки. Код выгружен на GitHub.
- [2026-07-28] 🛡️ **СТАБИЛЬНЫЙ РЕЛИЗ (v1.0-stable)**: Система полностью отлажена, проблемы с таймаутами и лимитами API устранены. Рабочий вариант программы зафиксирован в Git с помощью тега `v1.0-stable`. При возникновении критических ошибок в будущем, откат системы будет производиться именно к этой контрольной точке. Дальнейшая разработка (добавление новых фич) будет вестись поверх этой базы, не затрагивая зафиксированный слепок.
- [2026-07-28] Утвержден регламент отката: по кодовой фразе *"посмотри в журнале последнюю рабочую версию и откатись к ней"* система обязана предложить откат до последнего стабильного релиза (в данный момент это `git checkout v1.0-stable`).
- [2026-08-07] Выпущен стабильный релиз v2.0-modification с очисткой хардкод-скриптов и приватных данных.
- [2026-08-07] Подготовлено Docker-окружение для Ubuntu (VPS Beget): создан Dockerfile на базе python:3.13-slim с установкой tesseract-ocr, tesseract-ocr-rus, poppler-utils и ffmpeg/libsm6.
- [2026-08-07] Проанализирован `opencode.json`. Настроено подключение MCP SSH-сервера (mcp-server-ssh / ssh connector) для автоматического выполнения команд деплоя на VPS 159.194.232.74, с использованием безопасной ссылки на переменную `{env:BEGET_SSH_PASSWORD}`.
- [2026-08-08] Выполнен Шаг 1: Реализовано игнорирование системных файлов (_*) в folder_watcher.py и rag.py, публикация ресурсов на Яндекс.Диске через Yandex Disk API (PUT/POST + GET) с получением public_url и отправкой ссылки в email-уведомлении.
- [2026-08-08] Выполнен Шаг 2: Внедрен гибридный парсер документов с OCR (`document_parser.py`). Реализован ETL-пайплайн (парсинг документов, чанкинг, формирование и загрузка `_{имя_папки}_cache.json` на Яндекс.Диск через upload href API, публикация ссылки и переключение rag.py на чтение исключительно из JSON-кэша).
- [2026-08-08] Выполнен Шаг 3: Создана Docker-инфраструктура (`docker-compose.yml` со связкой Web + PostgreSQL 15 Alpine и персистентным томом `postgres_data`, файл шаблона окружения `.env.example`). Проект полностью подготовлен к локальному запуску и финальному E2E-тестированию.
- [2026-08-08] Выполнен автоматизированный деплой на Beget VPS (159.194.232.74) с использованием SSH. Успешно установлены Docker Engine и Docker Compose, собраны и запущены контейнеры `konsyltant_web` (FastAPI) и `konsyltant_db` (PostgreSQL 15).
- [2026-08-08] 🎉 **ФИНАЛЬНЫЙ ПРОДАКШЕН РЕЛИЗ (v3.0-stable-production)**: Успешно проведено 100% E2E-тестирование на Production VPS Beget. Гибридный парсер (OCR Tesseract), ETL-пайплайн с генерацией кэша на Яндекс.Диске и ИИ-модуль RAG безупречно работают с реальными файлами всех форматов (PDF, DOCX, PNG). Версия зафиксирована аннотированным тегом `v3.0-stable-production` и выгружена на GitHub как новая базовая точка отката (Restore Point).
- [2026-08-08] Внедрен Hotfix обработки ошибок (Graceful Degradation) в rag.py: вызовы GigaChat API обернуты в блок try/except с перехватом 401/403 и таймаутов, возвращается понятное сообщение для пользователя.
- [2026-08-08] 🛡️ **Внедрение Fault Tolerance и сетевых/OCR таймаутов (Defensive Programming)**: Проведена диагностика логов веб-сервиса на VPS. Внедрены жесткие сетевые таймауты (`timeout=15`) для всех вызовов `requests`, таймаут `timeout=60` для Tesseract OCR с постраничной изоляцией ошибок, а также изоляция сбоев цикла `folder_watcher.py` (`[ETL ERROR]`) для предотвращения падения фонового воркера. Изменения выгружены на GitHub и применены на Production VPS.
- [2026-08-11] Проведен полный аудит репозитория, создан и обновлен технический Канвас проекта (`ARCHITECTURE.md`) с подробным Roadmap из 4 фаз.
- [2026-08-11] Выполнен Step 1.1 (Security & RBAC Schema Foundation). Библиотека `cryptography` добавлена в `requirements.txt`. В `database.py` обновлена таблица `patient_access` (добавлены `role`, `is_verified` с обратной совместимостью) и создана новая таблица шеринга доступов `patient_share_grants` для связи пациентов и врачей.
- [2026-08-11] Выполнен Step 1.2 (Data at Rest Encryption Module). Создан модуль `crypto_utils.py` с автоматической генерацией временного ключа и перехватом `InvalidToken` для обратной совместимости. Написан модульный тест `test_crypto.py`. Обновлен `.env.example`.
- [2026-08-11] Выполнен Step 1.3 (Nginx Reverse Proxy & Deep Linking Configuration). Создана базовая маршрутизация в `nginx/default.conf` (проксирование `/` и `/app/` на FastAPI с таймаутами и поддержкой Deep Linking). Сервис `web` в `docker-compose.yml` закрыт от внешнего мира, добавлен новый сервис `nginx:alpine` (порт 80).
- [2026-08-11] Выполнен Phase 2 (Step 2.1: Public Portal Data Schema & REST Endpoints). В `database.py` добавлены таблицы `public_services`, `public_events`, `public_posts` и расширена таблица `patient_access` (поля профиля). Добавлен механизм автоматического сидирования (Seeding) БД тестовыми данными (включая фейкового врача). В `main.py` добавлены GET-эндпоинты `/api/v1/public/*` для публичного API.
- [2026-08-11] Выполнен Phase 2 (Step 2.2: Frontend Integration). Монолитный HTML разделен на Jinja2-шаблон (`templates/index.html`), стили (`static/css/style.css`) и скрипты (`static/js/bubbles.js`, `static/js/app.js`). В `app.js` встроена логика воспроизведения аудио при наведении на персонажей, а также логика Fallback-загрузки (Mock-данные при недоступности API). Обновлен `main.py` для использования `Jinja2Templates` (отдача на `/`), а старый чат перенесен на `/app/`. Настроена `nginx/default.conf` и `docker-compose.yml` для прямой раздачи статики (папка `static/`).
- [2026-08-14] Выполнена миграция системы на Stateless JWT-аутентификацию:
  - Библиотека `PyJWT` добавлена в `requirements.txt`.
  - В `.env.example`, `.env` и `copy.env` добавлены параметры `JWT_SECRET_KEY`, `JWT_ALGORITHM=HS256`, `ACCESS_TOKEN_EXPIRE_MINUTES=30`.
  - В `security_utils.py` (с реэкспортом в `crypto_utils.py`) реализованы функции `create_access_token` и `verify_token` с поддержкой payload (`sub`, `allowed_folder`, `exp`) и обработкой ошибок истечения срока / невалидных сигнатур.
  - Из `main.py` полностью удален глобальный словарь `SESSIONS = {}`.
  - Внедрена FastAPI зависимость `get_current_patient` (Bearer Header), защищающая роуты `/api/patient/files` и `/api/chat`.
  - Написан комплексный набор тестов `test_jwt_auth.py` и обновлен `test_api.py` (покрытие валидности, просроченных и поддельных токенов).
- [2026-08-14] Проведен аудит и завершена интеграция ТЗ по фронтенду (Рефакторинг лендинга и статики):
  - Монолитный HTML успешно разделен (`templates/index.html`, `static/css/style.css`, `static/js/bubbles.js`, `static/js/app.js`), очищен от лишних хвостов.
  - Все 6 изображений персонажей (`char_a.jpg`, `char_o.jpg`, `char_u.jpg`, `char_i.jpg`, `char_y.jpg`, `char_e.jpg`) размещены в `static/images/`, синхронизированы с `CHARACTERS` в `app.js` и поддержаны PNG/Emoji-фоллбэками.
  - Интегрированы аудиофайлы персонажей (`sound_a.mp3`, `sound_o.mp3`, `sound_u.mp3`, `sound_i.mp3`, `sound_y.mp3`, `sound_e.mp3`) в `static/audio/`.
  - Настроено монтирование `StaticFiles` и `Jinja2Templates` в FastAPI.
  - Проверена работа офлайн/демо фоллбэка (`MOCK_DATA`, бейдж демо-режима).
- [2026-08-15] Контейнеры пересобраны и успешно запущены в Docker (`konsyltant_web`, `konsyltant_db`, `konsyltant_nginx`). Все эндпоинты (HTML лендинг, статика CSS/JS/Images/Audio, публичные REST API) протестированы и возвращают HTTP 200 OK.
- [2026-08-17] Выполнен Phase 3 (Step 3.1: Documentation Fix & Doctor RBAC / Data Sharing Schema Foundation):
  - В `ARCHITECTURE.md` исправлено указание имени модуля парсера документов на `document_parser.py`.
  - В `database.py` внедрены таблицы `doctors` (профили врачей) и `patient_share_grants` (временные токены шеринга доступа к медкарте пациента) с автоматической миграцией схемы для PostgreSQL и SQLite.
  - Реализованы хелперы работы с базой данных: `create_doctor`, `get_doctor_by_id`, `verify_doctor`, `create_share_grant`, `validate_share_grant` (с поддержкой TTL и часовых поясов UTC).
  - Написан и успешно пройден модульный тестовый набор `test_doctor_sharing_schema.py` (создание, верификация, генерация и проверка токенов шеринга, обработка истекших/невалидных токенов — 100% OK).
- [2026-08-18] Внедрена интерактивная анимация и логика плавающего персонажа «Алик»:
  - Сгенерирован оптимизированный WebP с альфа-каналом прозрачности (`Alik.webp`, 19 КБ) из исходника `Alik.png`.
  - В `templates/index.html` разделены слои Hero-карточки (выделен контейнер `#hero-alik-wrapper` с `<picture>`) и добавлен плавающий виджет `#floating-alik-widget` с зеленым диалоговым облачком (`#alik-speech-bubble`) и кнопками управления.
  - Для всех 9 смысловых блоков страницы прописаны атрибуты `data-alik-comment` с дружелюбными репликами-подсказками по нейропсихологии и услугам центра.
  - Реализован 100% автономный (Self-hosted, без сторонних сервисов) механизм FLIP-анимации (First, Last, Invert, Play) на GPU-ускоренных свойствах `transform` и `opacity` с плавной интерполяцией между Hero-позицией и фиксированным виджетом.
  - Интегрирован `IntersectionObserver` для плавной смены текстовых реплик Алика без аудио-навязчивости.
  - Реализована адаптивность под мобильные экраны (компактный размер 82px, сворачивание по тапу/кнопке `−`/`+`).
  - Все изменения отправлены в GitHub-репозиторий и успешно задеплоены на боевой VPS (159.194.232.74). Все контейнеры и эндпоинты работают с кодом 200 OK.
- [2026-08-18] Выполнен Phase 3 (Step 3.2: Implement Doctor Auth & Data Sharing REST Endpoints):
  - В `main.py` добавлены Pydantic-схемы: `DoctorLoginRequest`, `DoctorAuthResponse`, `ShareGrantCreateRequest`, `ShareGrantResponse`.
  - Реализован FastAPI Dependency `get_current_doctor` с жесткой валидацией роли `DOCTOR` в Stateless JWT.
  - Внедрены REST-эндпоинты:
    - `POST /api/v1/doctor/login`: авторизация врача (`verify_doctor_credentials`) с выдачей Bearer JWT.
    - `POST /api/v1/patient/share`: защищенный токеном пациента эндпоинт выпуска временного токена шеринга (`patient_share_grants`) с настраиваемым сроком жизни.
    - `GET /api/v1/doctor/patient-records/{share_token}`: защищенный докторским JWT эндпоинт безопасного доступа к медкарте и документам пациента по валидному токену шеринга.
  - Написан интеграционный тестовый набор `test_doctor_api.py` (6 тестов: авторизация доктора, генерация шеринга пациентом, доступ доктора к карте, отклонение просроченных/невалидных токенов, ролевой RBAC — 100% PASS).
- [2026-08-18] Выполнен Phase 3 (Step 3.3: Implement Doctor Dashboard UI & Patient Share Link Components):
  - В интерфейсе пациента (`static/index.html`, `static/style.css`, `static/app.js`):
    - Добавлена кнопка «🔗 Поделиться картой с врачом» в шапку чата.
    - Разработано модальное окно `#share-modal` с выбором срока действия доступа (24ч, 48ч, 7 дней).
    - Интегрирована генерация защищенной ссылки через `POST /api/v1/patient/share` и копирование в буфер обмена (`navigator.clipboard.writeText`) с визуальным подтверждением.
  - В интерфейсе врача и главного портала (`templates/index.html`, `static/js/app.js`):
    - В подвал добавлена ссылка быстрого перехода «🩺 Кабинет врача».
    - Модальное окно авторизации врача `#doctor-modal` переведено на реальный API-вызов `POST /api/v1/doctor/login` с сохранением сессии и профиля.
    - Разработан полнофункциональный дашборд врача `#doctor-dashboard-modal`: профиль специалиста (ФИО, специализация, бейдж), поисковая строка по токену/ссылке, карточка медкарты пациента, сетка диагностических файлов (PDF/DOCX/выписки) с кнопками просмотра/скачивания и предупреждением о конфиденциальности.
    - Реализована глубокая маршрутизация (Deep-linking): авто-парсинг параметров `?share_token=...`, `#share_token=...` и хэша `#doctor`. При открытии ссылки неавторизованным врачом система запрашивает вход и сразу после логина автоматически открывает карту нужного пациента.
  - Разработан и успешно пройден сквозной интеграционный тест `test_e2e_doctor_share.py` (100% PASS).
- [2026-08-19] [Initial Audit] Первичный технический аудит рабочего пространства:
  - Проведен комплексный статический анализ кодовой базы, архитектуры, зависимостей, безопасности, схем данных и тестов.
  - Подтверждено стабильное состояние приложения: все 7 подсистем (FastAPI, Nginx, PostgreSQL, Stateless JWT, RAG/ETL, Doctor RBAC, SPA/SSR) синхронизированы и готовы к дальнейшей разработке.
  - Составлен детальный аналитический отчет для Архитектора.
  - Затронутые файлы: `ARCHITECTURE.md`, `process.md`.
- [2026-08-19] [Security] Внедрение Rate Limiting для endpoints авторизации:
  - Разработан и интегрирован легковесный потокобезопасный `InMemoryAuthRateLimiter` и HTTP-middleware в `main.py`.
  - Защищены эндпоинты авторизации: `/api/login` (пациенты), `/api/v1/doctor/login` (врачи), `/api/v1/admin/login` (администраторы).
  - Конфигурация: максимум 5 попыток в минуту с одного IP (с учетом заголовка `X-Forwarded-For`), период блокировки 5 минут (300 секунд).
  - Ответ при превышении: HTTP 429 Too Many Requests с телом `{"detail": "...", "retry_after": 300}` и заголовком `Retry-After: 300`.
  - Реализован автоматический сброс счетчика попыток при успешной авторизации (HTTP 200) и маскирование IP-адресов в логах (`mask_ip`).
  - Разработан тестовый набор `test_rate_limiting.py` (6/6 тестов PASS).
  - Затронутые файлы: `main.py`, `security_utils.py`, `test_rate_limiting.py`.
- [2026-08-19] [Performance] Оптимизация производительности БД и создание индексов с автомиграцией:
  - Проведен аудит SQL-запросов (`patient_access`, `patient_share_grants`, `doctors`, `public_posts`, `public_leads`, `public_services`, `public_events`).
  - Разработан механизм автомиграции индексов `ensure_indexes()` в `database.py`, встроенный в жизненный цикл инициализации `init_db()`.
  - Созданы уникальные B-tree индексы для токенов доступа (`idx_patient_access_token`, `idx_share_grants_token`), обеспечивающие $O(\log N)$ время отклика при авторизации и валидации шеринг-ссылок врачей.
  - Созданы составные индексы (Composite Indexes): `idx_share_grants_patient_active`, `idx_patient_access_role_verified`, `idx_public_leads_status_created`.
  - Созданы индексы сортировки и внешних ключей (`idx_public_posts_created_at`, `idx_share_grants_doctor_id`, `idx_doctors_license_number`, `idx_doctors_full_name`, `idx_public_services_category`, `idx_public_events_date`).
  - Разработан тестовый набор `test_db_indexes.py` (3/3 тестов PASS).
  - Затронутые файлы: `database.py`, `test_db_indexes.py`.
- [2026-08-19] [Tech Sovereignty] Аудит суверенитета, документирование бизнес-логики и реорганизация репозитория:
  - Задокументирован сквозной 5-этапный бизнес-процесс обработки медицинских карт на Яндекс.Диске и принцип строгой изоляции контекста RAG (каждая папка пациента = изолированный тенант).
  - Зафиксирован архитектурный императив технологического суверенитета (функционирование на 100% отечественном стеке) и составлена матрица устойчивости внешних интеграций к блокировкам РКН и санкциям.
  - Проведен аудит зависимостей: подтверждено 100% отсутствие импортов отладочных скриптов в production-коде.
  - Реорганизована структура репозитория: созданы каталоги `scripts/admin/` (перемещен `reset_db.py`), `scripts/debug/` (перемещены скрипты инспекции Диска), удалены устаревшие временные файлы (`extract.py`, `split_js.py`, `temp_script.js`, `index.html.лендинг`).
  - Создан документ `scripts/README.md` и сформирован `.dockerignore` для исключения служебных скриптов из production-сборок.
  - Затронутые файлы: `ARCHITECTURE.md`, `scripts/README.md`, `.dockerignore`.
- [2026-08-19] [Auth Fix] Экстренная починка авторизации врача и создание сид-скрипта:
  - Выявлена причина сбоя: таблица `doctors` в production PostgreSQL не содержала колонок `password_hash`, `email` и `role`, а `DoctorLoginRequest` требовал строго поле `login`.
  - Проведена автомиграция: добавлены колонки `email`, `password_hash`, `role` в таблицу `doctors` как для PostgreSQL, так и для SQLite.
  - В `DoctorLoginRequest` добавлена универсальная поддержка полей `email`, `login`, `username` через вычисляемое свойство `identifier`.
  - Функция `verify_doctor_credentials` расширена для поиска по `email`, `license_number`, `id`, `full_name` и проверки bcrypt-хэша пароля.
  - Создан скрипт `scripts/admin/seed_producer_doctor.py` (`producer@cmz.site` / `TestAccess2026!`).
  - Затронутые файлы: `database.py`, `main.py`, `scripts/admin/seed_producer_doctor.py`.
- [2026-08-20] [AI Clinical Summary] Генерация структурированного медицинского резюме пациента:
  - Разработан RAG-пайплайн клинической суммаризации `generate_medical_summary` в `rag.py` на базе извлеченных чанков медкарты пациента.
  - Реализован строгий zero-hallucination промпт со структурированным JSON-ответом (`anamnesis`, `diagnoses`, `contraindications`, `drug_interactions`, `recommendations`).
  - Создан эндпоинт `POST /api/v1/doctor/patient/{patient_folder_id}/summary` с двухфакторной защитой (JWT роль `DOCTOR` + проверка активного гранта в `patient_share_grants`).
  - Разработан тестовый набор `test_doctor_summary_api.py` (6/6 тестов PASS).
  - Затронутые файлы: `rag.py`, `main.py`, `test_doctor_summary_api.py`.
- [2026-08-20] [Feature] Генерация клинического PDF-отчета (ReportLab):
  - Создан модуль `pdf_generator.py` на базе ReportLab с интеграцией шрифтов DejaVuSans (полная поддержка кириллицы UTF-8).
  - Разработан двухпроходный класс `NumberedCanvas` для нумерации страниц («Стр. X из Y»), стилизованная шапка клиники, акцентные блоки противопоказаний (`Alert Box`), таблицы медикаментов и юридический дисклеймер.
  - Реализован эндпоинт `GET /api/v1/doctor/patient/{patient_folder_id}/summary/pdf` с отдачей бинарного потока `application/pdf` и заголовком `Content-Disposition: attachment`.
  - Разработан тестовый набор `test_pdf_generation.py` (5/5 тестов PASS).
  - Затронутые файлы: `pdf_generator.py`, `main.py`, `requirements.txt`, `test_pdf_generation.py`.
- [2026-08-20] [Security] Ограничение лимита активных шеринг-ссылок пациента и механизм отзыва:
  - Реализовано бизнес-правило: максимум 2 активные шеринг-ссылки одновременно на одного пациента.
  - Внедрена функция `count_active_shares(patient_folder_id)` на основе составного индекса `idx_share_grants_patient_active`.
  - При превышении лимита эндпоинт `POST /api/v1/patient/share` возвращает `HTTP 429 Too Many Requests`.
  - Добавлены эндпоинты `GET /api/v1/patient/shares` (список активных ссылок) и `DELETE /api/v1/patient/share/{grant_id}` (мягкий отзыв доступа).
  - Разработан тестовый набор `test_sharing_limit.py` (1/1 тестов PASS).
  - Затронутые файлы: `database.py`, `main.py`, `test_sharing_limit.py`.
- [2026-08-20] [Tech Sovereignty] Полная деинсталляция Google Drive API и автономность Яндекс.Диска:
  - Полностью удалена интеграция с Google Drive API как нарушающая архитектурный императив технологического суверенитета.
  - Удалено: `google-api-python-client`, `google-auth` из `requirements.txt`, `credentials.json` из репозитория, файлы `drive_api.py` и `test_drive.py`.
  - Все fallback-маршруты в `main.py` переведены исключительно на Яндекс.Диск.
  - Создан эндпоинт проверки здоровья хранилища `GET /api/v1/health/yandex-disk` с JWT-защитой (`ADMIN`).
  - Разработан тестовый набор `test_yandex_disk_autonomy.py` (7/7 тестов PASS).
  - Затронутые файлы: `requirements.txt`, `main.py`, `rag.py`, `folder_watcher.py`, `test_yandex_disk_autonomy.py`.
- [2026-08-21] [ETL Diagnostic] Диагностика ETL-конвейера, исключение папок и тестовый пациент:
  - Добавлен механизм исключения системных папок из сканирования (`EXCLUDED_FOLDERS=Загрузки,Trash,Archive,Корзина`).
  - Добавлены пошаговое логирование каждого этапа ETL в `LAST_ETL_LOGS`.
  - Создан диагностический эндпоинт для администратора `GET /api/v1/admin/diagnose/folder/{folder_name}`.
  - Исправлен формат генерации клиентских ссылок (`https://цмз.site/app/?token={token}`) и настроен бесшовный редирект для `/app/{token}`.
  - Создан идемпотентный скрипт сидирования тестового пациента `scripts/admin/seed_test_patient.py`.
  - Разработан тестовый набор `test_etl_diagnostic.py` (4/4 тестов PASS).
  - Затронутые файлы: `folder_watcher.py`, `database.py`, `main.py`, `.env.example`, `scripts/admin/seed_test_patient.py`, `test_etl_diagnostic.py`.
- [2026-08-21] [Observability] Подсистема наблюдаемости: метрики скорости ETL и учет токенов GigaChat:
  - Внедрена таблица `etl_metrics` с автомиграцией и индексами в `init_db()` и сбор метрик в `folder_watcher.py` (длительность, число файлов, страниц, чанков, ошибок, ср. время на файл).
  - Создан административный эндпоинт `GET /api/v1/admin/etl/metrics` (история и агрегаты) и расширен диагностический эндпоинт `GET /api/v1/admin/diagnose/folder/{folder_name}` полем `last_etl_metrics`.
  - Внедрена таблица `llm_usage` с автомиграцией и автоматическая фиксация `prompt_tokens`, `completion_tokens`, `total_tokens` после каждого вызова GigaChat в `rag.py`.
  - Создан административный эндпоинт `GET /api/v1/admin/llm/usage` (потребление за сегодня, 7 дней, 13 дней, все время, разбивка по моделям и типам запросов).
  - Интегрирован официальный метод GigaChat `GET /balance` с graceful обработкой (200 OK vs 403 Pay-As-You-Go) и опциональным лимитом купленного пакета `GIGACHAT_PACKAGE_TOKENS_LIMIT`.
  - Разработан тестовый набор `test_observability_metrics.py` (6/6 тестов PASS).
  - Затронутые файлы: `database.py`, `folder_watcher.py`, `rag.py`, `main.py`, `test_observability_metrics.py`.

## План
- **Что сделано:**
  - Зафиксирована стабильная точка отката: создан и отправлен в GitHub тег `v6.0-stable-cms-landing` (полный комплекс: лендинг, персонажи, звуки, экспертный блог, сбор заявок, панель управления CMS, полноэкранная анимация пузырьков, домен `цмз.site`, автомиграции БД).
  - Реализована интерактивная полноэкранная Canvas-анимация снов (`bubbles.js`) со световыми градиентами и отталкиванием мыши, плавно покрывающая всю страницу.
  - Написаны и засидированы 3 полноценные экспертные статьи по детской нейропсихологии и запуску речи («Как мягко подтолкнуть речь», «Энергия в мирное русло», «Быстро устает и отвлекается в школе»).
  - Реализован модальный ридер для чтения полных текстов статей (`#article-modal`).
  - Создана система сбора лидов/заявок с лендинга (`public_leads`, `POST /api/v1/public/leads`) с валидацией и уведомлением пользователя.
  - Разработана и интегрирована Панель Администратора (CMS):
    - Stateless JWT авторизация с ролью `ADMIN` (`POST /api/v1/admin/login`).
    - Мониторинг входящих заявок родителей в реальном времени (`GET /api/v1/admin/leads`).
    - Полный CRUD статей блога (`POST/PUT/DELETE /api/v1/admin/posts`).
  - Реализована полная адаптация под мобильные устройства и Android-смартфоны:
    - Для `#hero-canvas` задано `pointer-events: none !important; touch-action: none;` — устранена блокировка скролла на сенсорных экранах.
    - В `bubbles.js` добавлены обработчики `touchstart` и `touchmove` с флагом `{ passive: true }` для плавного 60fps скролла без задержек.
    - В `app.js` воспроизведение аудио персонажей вынесено в защищенную функцию `playCharSound` с обработкой ошибок Android Autoplay Policy и поддержкой воспроизведения по тапу (`click`/`touchstart`).
    - Оптимизирована верстка под экраны 360px–430px: резиновые кнопки персонажей, адаптивные модальные окна со скроллом (`max-height: 92vh`, `-webkit-overflow-scrolling: touch`), фиксированный размер шрифта 16px на инпутах для предотвращения автозума.
  - Развернута инфраструктура HTTPS / SSL:
    - Конфигурация Nginx обновлена с поддержкой порта 443, современных протоколов `TLSv1.2 TLSv1.3` и Let's Encrypt ACME challenge (`/.well-known/acme-challenge/`).
    - Добавлен скрипт `nginx/init-ssl.sh` для автоматической генерации фоллбэк-сертификата при первом запуске (предотвращает падение Nginx при отсутствии боевого сертификата).
    - В `docker-compose.yml` добавлен сервис `certbot` с автообновлением сертификатов и проброшены порты `80` и `443`.
  - Исправлен Punycode домена в конфигурации Nginx (`nginx/default.conf`) и скрипте SSL (`init-ssl.sh`) на точный `xn--g1aj3a.site` (цмз.site), что обеспечивает корректное подключение выпущенного SSL-сертификата Let's Encrypt.
  - Выполнен Step 3.1: заложена схема профилей докторов и шеринга данных по временным токенам (`doctors`, `patient_share_grants`), реализованы хелперы и 100% покрыты тестами.
  - Реализован плавающий персонаж «Алик»: FLIP-анимация между Hero и фиксированным слоем, зеленое диалоговое облачко со сменой реплик по секциям, поддержка WebP, адаптивность и автономность без CDN.
  - Выполнен Step 3.2: разработаны и протестированы эндпоинты авторизации врачей и шеринга данных (`/api/v1/doctor/login`, `/api/v1/patient/share`, `/api/v1/doctor/patient-records/{share_token}`).
  - Выполнен Step 3.3: созданы UI-компоненты шеринга в SPA пациента, кабинет врача с поиском и просмотром медкарты, глубокие ссылки (`?share_token=...`) и обработка ошибок доступа.
  - Внедрен Rate Limiting на всех эндпоинтах авторизации (`/api/login`, `/api/v1/doctor/login`, `/api/v1/admin/login`).
  - Выполнена оптимизация производительности БД: созданы B-tree индексы с автомиграцией (`ensure_indexes`).
  - Задокументирован сквозной бизнес-процесс Яндекс.Диска, директива технологического суверенитета и реорганизована структура `scripts/`.
  - Реализована подсистема наблюдаемости: замер производительности ETL-конвейера (`etl_metrics`), учёт потребления токенов GigaChat (`llm_usage`), интеграция с официальным методом Сбера `GET /balance` и защищенные административные эндпоинты `/api/v1/admin/etl/metrics` и `/api/v1/admin/llm/usage`.
- [2026-08-21] [CMS & UI] Переструктуризация лендинга, админский доступ Продюсера и операционная панель:
  - Создан идемпотентный скрипт сидирования администратора `scripts/admin/seed_producer_admin.py` (`producer-admin@cmz.site` / `AdminAccess2026!` с ролью `ADMIN` и bcrypt-хэшированием пароля).
  - В админ-CMS добавлен раздел «Операционная панель» (`#admin-tab-ops`), отображающий карточки состояния Яндекс.Диска, метрики ETL-конвейера, расход токенов LLM (за день, неделю, все время) и официальные остатки баланса GigaChat.
  - Разработан тестовый набор `test_landing_and_admin_ops.py` (5/5 тестов PASS).
  - Затронутые файлы: `templates/index.html`, `static/js/app.js`, `scripts/admin/seed_producer_admin.py`, `test_landing_and_admin_ops.py`.
- [2026-08-21] [CMS / Multimedia] Экстренная починка CRUD-операций, поддержка мультимедиа и перенос постов на 1 место:
  - Проведена live-диагностика на VPS: база PostgreSQL и бэкенд FastAPI полностью исправны; сбой происходил на фронтенде из-за потери токена в `sessionStorage`. Внедрено гибридное чтение токена `getAdminToken()` (`sessionStorage` + `localStorage`).
  - Расширена таблица `public_posts` полями `cover_image_url` (VARCHAR 500), `video_url` (VARCHAR 500), `attachments` (TEXT/JSON) с автомиграцией схем PostgreSQL и SQLite.
  - Создана таблица `public_library` (`id`, `title`, `summary`, `content`, `category`, `tags`, `cover_image_url`, `video_url`, `attachments`, `created_at`) с индексами `idx_public_library_created_at`, `idx_public_library_category`.
  - В `folder_watcher.py` добавлен модуль загрузки медиафайлов на Яндекс.Диск `upload_media_file_to_yandex_disk` в папку `disk:/uploads/` с автоматической публикацией.
  - Создан защищенный эндпоинт `POST /api/v1/admin/upload` для загрузки изображений и видео.
  - В `requirements.txt` добавлена библиотека `python-multipart`.
  - Секция «Новые посты» (`#posts`) перемещена на САМОЕ ПЕРВОЕ МЕСТО на лендинге (выше Hero-блока):
    1. «Новые посты» (`#posts`)
    2. Hero-блок («Детская вселенная: Маленькая страна», `#hero`)
    3. «Полезная библиотека» (`#blog`)
    4. «Персонажи звуков» (`#characters`)
    5. «О центре» (`#about`)
    6. «Направления заботы» (`#services`)
    7. «Волшебники Центра» (`#doctors`)
    8. «События & Жизнь центра» (`#events`)
    9. «Особая забота» (`#special-care-section`)
    10. «Ждем вас в гости!» (`#contacts`)
    11. Подвал (`footer`)
  - Подтверждено 0 упоминаний слова «клиника» и слова «музыкальная» в пользовательском интерфейсе.
  - Разработан расширенный тестовый набор `test_landing_and_admin_ops.py` (7/7 тестов PASS).
  - Затронутые файлы: `database.py`, `folder_watcher.py`, `main.py`, `templates/index.html`, `static/js/app.js`, `requirements.txt`, `test_landing_and_admin_ops.py`.
- [2026-08-21] [Alert System] Система мониторинга и оповещений о критических сбоях с дублированием:
  - Разработан модуль `alert_service.py` с фоновым воркером `alert_worker_loop` (периодичность 5 минут).
  - Настроено дублирование уведомлений одновременно на 2 адреса: `PRIMARY_ALERT_EMAIL=konsultantms@yandex.com` и `SECONDARY_ALERT_EMAIL=sergo123qwe321@gmail.com`.
  - Реализован мониторинг 6 критических метрик с порогами срабатывания:
    1. Недоступность Яндекс.Диска (ошибка API при проверке квоты)
    2. Недоступность GigaChat API (3 последовательные ошибки LLM или OAuth)
    3. Падение фонового ETL-воркера folder_watcher (отсутствие heartbeat > 10 мин)
    4. Остаток токенов GigaChat ниже 20% от исходного пакета
    5. Средняя скорость ETL выше 15 с/файл (деградация производительности)
    6. Недоступность базы данных PostgreSQL более 30 секунд
  - Реализован механизм дедупликации (не чаще 1 раза в час по одной проблеме) и однократные уведомления о выздоровлении при восстановлении сервиса.
  - Создан защищенный эндпоинт `POST /api/v1/admin/alerts/test` (JWT `ADMIN`) и эндпоинт `GET /api/v1/admin/alerts/status`.
  - В операционную панель CMS добавлена карточка мониторинга и кнопка «Проверить оповещения (Тест)» с живым статусом отправки.
  - Разработан тестовый набор `test_alert_system.py`.
- [2026-08-21] [Render Infrastructure Audit] Экстренный аудит инфраструктуры Render.com и подтверждение полной автономии VPS:
  - Проведен детальный аудит конфигураций, переменных окружения и баз данных на боевом сервере VPS Beget (`159.194.232.74`).
  - Подтверждено, что боевая СУБД PostgreSQL (`konsyltant_db`) работает на собственном VPS внутри изолированного Docker-контейнера `konsyltant_db` с локальным хранилищем `postgres_data`.
  - Установлено наличие в локальной базе VPS всех актуальных данных: 28 записей пациентов (включая «Дюзгёрен Арон Альп», «Тестовый Пациент» и всех реальных пациентов), 75 шеринг-грантов, 3 врачей, 8 постов блога, 6 логов ETL и 31 записи использования LLM.
  - Определена роль Render: это устаревший тестовый стенд этапа прототипирования (июль 2026 г.), полностью отключенный от боевого контура после миграции на собственный VPS 8 августа 2026 г.
  - Диагностирована причина сбоя сборки на Render: вебхук GitHub инициирует автоматический билд в бесплатном тарифе Render, где нет системных библиотек OCR Tesseract / Poppler и превышается лимит RAM 512 МБ (OOM).
  - Сформирован план отключения заброшенных сервисов Render через веб-панель. Подтвержден нулевой риск для продакшена и сохранности медицинских данных.
  - Затронутые файлы: `process.md`, `ARCHITECTURE.md`.
- [2026-08-21] [Phase 4 / Voice Input] Интеграция голосового ввода через Web Speech API в чате пациента и синхронизация ARCHITECTURE.md:
  - Разработан автономный клиентский модуль `static/js/voiceInput.js` с функцией детекции `isSpeechRecognitionSupported()` (`window.SpeechRecognition`, `window.webkitSpeechRecognition`).
  - В DOM приватного чата (`static/index.html`) добавлена кнопка микрофона `#voice-input-btn`, статус-бар `#voice-status-container` с индикатором «Слушаю...» и фоллбэк-сообщение для неподдерживаемых браузеров.
  - Реализованы 3 визуальных состояния кнопки: неактивна (серая), запись (пульсирующая красная анимация с индикатором `pulseMic`), ошибка доступа/сети (желтая с подсказкой).
  - Сконфигурирован `SpeechRecognition`: язык `ru-RU`, непрерывная запись `continuous=true`, живые промежуточные результаты `interimResults=true` с добавлением в `chat-input`.
  - Реализована обработка ошибок: отказ в доступе `not-allowed`, ошибка сети `network`, таймаут тишины `no-speech` (автоостановка без шума), прерывание `aborted`.
  - Внедрены UX-улучшения: автофокус и пульсирующая рамка поля ввода (`recording-focus`), автоостановка при ручном вводе с клавиатуры, 60-секундный таймаут тишины.
  - В `ARCHITECTURE.md` удалены дубликаты структуры файлов, окончательно подтверждена деинсталляция Google Drive и зафиксировано завершение Phase 4 Roadmap.
  - Разработан тестовый набор `test_voice_input.py` (6/6 тестов PASS).
  - Затронутые файлы: `static/js/voiceInput.js`, `static/index.html`, `static/style.css`, `static/app.js`, `static/js/app.js`, `ARCHITECTURE.md`, `process.md`, `test_voice_input.py`.
- [2026-08-21] [Production Launch Preparation] Подготовка к боевому тестированию на реальных данных пациента:
  - Создан идемпотентный боевой seed-скрипт `scripts/admin/seed_production_patient.py` (принимает имя папки как CLI аргумент, проверяет ее наличие на Яндекс.Диске, формирует RAG-кэш, генерирует токен и пароль, выводит учетные данные в консоль).
  - Проверена непрерывная работа ETL-воркера `folder_watcher` в логах боевого контейнера VPS (сканирование каждые 60 секунд, средняя скорость 4 с/файл).
  - Проведена проверка отправки тестовых писем через `notification_service.py` на оба адреса (`konsultantms@yandex.com` и `sergo123qwe321@gmail.com`) с добавлением хелперов `send_dual_email` и `send_email_to_recipient`.
  - Проверена подсистема оповещений (`POST /api/v1/admin/alerts/test`) и механизм дедупликации алертов.
  - Создана подробная пошаговая инструкция `PRODUCTION_LAUNCH.md` для Продюсера по загрузке папок, ETL-обработке, диагностике и регламенту при сбоях.
  - Проведен финальный аудит безопасности: эндпоинты `/api/v1/patient/share`, `/api/v1/doctor/patient/{folder_id}/summary` (GET/POST) и `/api/v1/doctor/patient/{folder_id}/summary/pdf` защищены JWT и проверкой активных грантов шеринга, Rate Limiter активен.
  - Добавлен раздел «12. Production Launch Checklist» в `ARCHITECTURE.md`.
  - Разработан и пройден тестовый набор `test_production_launch.py` (5/5 тестов PASS).
  - Затронутые файлы: `scripts/admin/seed_production_patient.py`, `notification_service.py`, `main.py`, `PRODUCTION_LAUNCH.md`, `ARCHITECTURE.md`, `process.md`, `test_production_launch.py`.

- [2026-08-25] [Release v7.0-production] Реализация блоков А, Б, В, Г, Д и аудит технологического суверенитета:
  - Блок А (Кабинет врача):
    - Реализован эндпоинт GET /api/v1/doctor/patient-records/{share_token}/document/{filename} для просмотра и скачивания оригинальных диагностических файлов по защищенному токену доступа.
    - Внедрена таблица doctor_notes (id, doctor_id, patient_folder_id, note_text, created_at, updated_at) с B-tree индексом idx_doctor_notes_doc_patient и полным CRUD API.
    - В дашборде врача реализованы: поле клинических заметок с автосохранением, кнопка «Сгенерировать резюме» с индикатором «Консилиум анализирует документы...», структурированные карточки заключения (анамнез, диагнозы, противопоказания красным, взаимодействия препаратов янтарным, рекомендации) и кнопка «Скачать PDF».
  - Блок Б (Локальное хранилище обложек и медиа):
    - Эндпоинт POST /api/v1/admin/upload переведен на локальное сохранение в static/uploads/ с прямой раздачей через Nginx.
    - Из folder_watcher.py удалена неиспользуемая функция upload_media_file_to_yandex_disk.
    - В .gitignore добавлено исключение для static/uploads/* с сохранением .gitkeep.
    - В docker-compose.yml подключен том uploads_data для персистентности файлов между перезапусками контейнеров web и nginx.
    - Выполнена миграция старых ссылок на обложки в базе данных (scripts/admin/migrate_cover_images.py).
  - Блок В (Гибридный видеоплеер с двойной ссылкой):
    - Разработана универсальная функция renderMediaBlock(videoUrl, coverImageUrl):
      - Локальные видео (.mp4/.webm) отображаются через HTML5 video controls poster.
      - Внешние ссылки (Rutube, VK Video, YouTube, Яндекс.Видео) трансформируются во встраиваемый iframe без автовоспроизведения.
      - Под каждым видеоплеером без исключения размещена текстовая ссылка «Смотреть на первоисточнике» с переходом на оригинальный URL.
  - Блок Г (Открытый чат сообщества на лендинге):
    - Внедрена таблица public_chat_messages (id, author_role, author_id, author_name, message_text, created_at) с индексом idx_public_chat_messages_created_at.
    - Реализовано публичное чтение ленты сообщений (GET /api/v1/public/chat) без авторизации.
    - Реализована отправка сообщений (POST /api/v1/public/chat) только для авторизованных пользователей любой роли (PATIENT, DOCTOR, ADMIN) с защитой от спама через Rate Limiting.
    - Внедрена строгая цветовая маркировка авторов: родители — зеленым, врачи — красным, администрация — фиолетовым.
    - На лендинге добавлены 3 точки входа (Родитель, Врач, Администратор) и модальное окно #parent-modal.
    - Реализована административная модерация (DELETE /api/v1/public/chat/{message_id}).
    - Подтверждено отсутствие ИИ-токенов в открытом чате.
  - Блок Д (Тестирование, суверенитет и релиз):
    - Разработаны и успешно пройдены 4 новых тестовых набора: test_doctor_notes_and_dashboard.py, test_local_uploads.py, test_community_chat.py, test_technological_sovereignty.py.
    - Автоматизированный аудит подтвердил 0 упоминаний обходных средств по всему репозиторию.
    - Полный тестовый набор из 85 тестов выполнен со 100% успехом.
    - Затронутые файлы: database.py, main.py, folder_watcher.py, docker-compose.yml, .gitignore, templates/index.html, static/js/app.js, test_doctor_notes_and_dashboard.py, test_local_uploads.py, test_community_chat.py, test_technological_sovereignty.py, scripts/admin/migrate_cover_images.py, process.md, ARCHITECTURE.md.

## План
- **Что сделано:**
  - Полностью реализованы и верифицированы блоки А, Б, В, Г, Д.
  - Все 85 тестов пройдены успешно.
- **Что в процессе:**
  - Деплой релиза v7.0-production на боевой VPS Beget.
- **Что предстоит:**
  - Создание релизного тега v7.0-production в ветке main.
