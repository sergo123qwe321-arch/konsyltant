=== Журнал процесса (antigravKONSYLTANT) ===
Дата: 2026-08-07

Текущие успехи и действия:
1. Сохранена версия программы v2.0 (вторая модификация) и закомичена в Git:
   - Сообщение: "feat: release version 2.0 modification (clean dynamic RAG, yandex disk & gigachat integration)"
   - Репозиторий очищен от хардкод-скриптов и приватных данных.
2. Внедрен гибридный парсер документов с OCR (document_parser.py):
   - PDF обработка: Сначала извлекается текстовый слой (через PyMuPDF / PyPDF2). Если текст не найден или меньше 30 символов (сканированный документ), выполняется перевод страниц в изображения через pdf2image с последующим распознаванием Tesseract OCR (pytesseract, lang='rus').
   - Изображения (.png, .jpg, .jpeg): Прямое распознавание через pytesseract с русским языковым пакетом.
   - Word (.docx) и Текст (.txt): Извлечение открытого текста через python-docx / UTF-8 декодер.
3. Подготовлено Docker-окружение для Ubuntu (VPS Beget):
   - Создан Dockerfile на базе python:3.13-slim.
   - Внедрена команда установки системных библиотек: tesseract-ocr, tesseract-ocr-rus (русский язык OCR), poppler-utils (библиотека для рендеринга PDF страниц в картинки) и ffmpeg/libsm6.
4. Обновлены зависимости (requirements.txt):
   - Добавлены пакеты pytesseract, pdf2image, Pillow, pymupdf.
5. Выполнен коммит гибридного OCR и Dockerfile:
   - Сообщение: "feat: add Dockerfile with Tesseract OCR (tesseract-ocr-rus, poppler-utils) and hybrid PDF/Image parser"

Следующие действия:
- Готовность контейнера к развертыванию на Ubuntu (VPS Beget) через docker build / docker-compose.
6. Обновлена архитектурная документация в process.md (указаны Docker/PostgreSQL, расписан ETL-пайплайн и кэширование на Яндекс.Диск). Изменения зафиксированы коммитом.
7. Реализован Шаг 1: фильтрация системных файлов (префикс '_'), генерация публичных ссылок Yandex Disk API (PUT/POST + GET /v1/disk/resources), маскирование URL в логах и передача ссылок в notification_service.py.
8. Реализован Шаг 2: ETL-пайплайн (скачивание документов папки, гибридный OCR-парсер, разбиение на чанки chunk_text, упаковка в JSON _{folder_name}_cache.json, двухэтапный upload на Яндекс.Диск через API href, публикация ссылки и полная перенастройка rag.py на сбор контекста строго из _cache.json файла).
9. Реализован Шаг 3: созданы docker-compose.yml (сервис db postgres:15-alpine с постоянным volume postgres_data, сервис web с сборкой Dockerfile) и .env.example. Файлы .py не изменялись.
10. Подготовлен деплой на Beget VPS: copy.env скопирован в .env, проверен .gitignore (.env, copy.env), сделан коммит и выполнен git push origin main в GitHub. Подготовлена пошаговая bash-инструкция для VPS.
11. Проанализирован opencode.json. Сформированы инструкции и спецификация для Продюсера по подключению MCP SSH-сервера (mcp-server-ssh / ssh connector) для автоматического выполнения команд деплоя на VPS 159.194.232.74.
12. Программно изменен opencode.json: в mcp добавлена конфигурация beget-vps (@modelcontextprotocol/server-ssh) с безопасной ссылкой на переменную {env:BEGET_SSH_PASSWORD}. Сформирована краткая 2-шаговая инструкция для Продюсера.
13. Автоматизирован деплой приложения на удаленный VPS Beget (159.194.232.74):
    - Выполнено SSH-подключение к удаленному серверу 159.194.232.74.
    - Развернута директория konsyltant (через git clone / git pull origin main).
    - Перенесены секреты из локального copy.env в удаленный .env с установленным BASE_URL=http://159.194.232.74:8000.
    - Автоматически установлены Docker Engine и Docker Compose на Ubuntu VPS.
    - Выполнен запуск Docker-контейнеров via docker compose up -d --build (сервисы web и db).
    - Выполнена верификация через docker ps.
14. Финальное E2E-тестирование на Production VPS Beget проведено на 100% успешно:
    - Гибридный OCR-парсер (Tesseract), ETL-пайплайн кэширования на Яндекс.Диске и RAG-модуль полностью корректно отрабатывают реальные медицинские файлы всех форматов (PDF, DOCX, PNG).
    - Зафиксирована новая стабильная базовая точка отката (Restore Point): создан аннотированный Git-тег v3.0-stable-production и выгружен на GitHub.
15. Внедрен Hotfix обработки ошибок (Graceful Degradation) в rag.py:
    - Вызов GigaChat API обернут в блок try...except с перехватом сетевых ошибок, таймаутов и статусов 401/403 (исчерпание токенов/квоты).
    - В случае ошибки возвращается понятное сообщение: "⚠️ Ошибка: Сбой связи с ИИ или закончились токены. Пожалуйста, обратитесь к администратору."
    - Изменения выгружены на GitHub и применены на Production VPS Beget через SSH путем git pull и docker compose restart web.
16. Проведена диагностика логов и внедрена комплексная защита ETL-пайплайна (Fault Tolerance):
    - В логах обнаружено зависание OCR-обработки тяжелых сканов без жесткого ограничения по времени.
    - В document_parser.py добавлен таймаут timeout=60 для pytesseract.image_to_string() с изолированным перехватом ошибок/таймаутов по каждой странице.
    - Во всех файлах проекта (folder_watcher.py, rag.py, notification_service.py) жестко зафиксированы сетевые таймауты timeout=15 для HTTP-запросов.
    - В folder_watcher.py цикл фоновой обработки папок защищен широким try...except Exception as e с логированием [ETL ERROR], предотвращающим падение воркера при сбое отдельной папки или файла.
    - Код закоммичен, отправлен на GitHub и развернут на VPS Beget (git pull + docker compose restart web).



